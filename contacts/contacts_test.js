'use strict';

describe('contactsApp.contacts module', function() {

  beforeEach(module('contactsApp.contacts'));

  describe('contacts controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ContactsCtrl = $controller('ContactsCtrl');
      expect(ContactCtrl).toBeDefined();
    }));

  });
});